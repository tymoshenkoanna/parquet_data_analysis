# parquet_data_analysis
Working with the parquet files that contain already scrapped and partially cleaned data, removing duplicates, defining important outputs, merging it with the inputs from other sources
